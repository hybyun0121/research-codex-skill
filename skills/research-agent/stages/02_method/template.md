# Method

## Method Claim

${method_claim}

## Connection To Motivation

- Selected direction: ${selected_direction}
- Motivation gap addressed: ${motivation_gap}
- Novelty claim: ${novelty_claim}
- Why this method should address the gap: ${motivation_connection}

## Inputs And Outputs

- Inputs: ${inputs}
- Outputs: ${outputs}
- Constraints: ${constraints}

## Notation Table

| Symbol | Meaning | Shape / Domain | Notes |
| --- | --- | --- | --- |
| ${symbol} | ${meaning} | ${shape_or_domain} | ${notation_notes} |

## Assumptions

| Assumption | Why It Is Needed | User Confirmation |
| --- | --- | --- |
| ${assumption} | ${assumption_reason} | ${confirmation_status} |

## Mathematical Formulation

```text
${formulation}
```

## Objective

```text
${objective}
```

## Algorithm Box

```text
${algorithm}
```

## Diagram / Visualization Plan

- Visual target: ${visual_target}
- Key components to show: ${visual_components}
- Relationship or flow to emphasize: ${visual_flow}

## Plain-language Explanation

${plain_language_explanation}

## Rigor Review

| Check | Status | Finding | Action |
| --- | --- | --- | --- |
| Undefined symbols | ${undefined_symbols_status} | ${undefined_symbols_finding} | ${undefined_symbols_action} |
| Dimensional consistency | ${dimension_status} | ${dimension_finding} | ${dimension_action} |
| Assumptions | ${assumption_status} | ${assumption_finding} | ${assumption_action} |
| Objective validity | ${objective_status} | ${objective_finding} | ${objective_action} |
| Data leakage | ${leakage_status} | ${leakage_finding} | ${leakage_action} |
| Novelty alignment | ${novelty_status} | ${novelty_finding} | ${novelty_action} |
| Motivation alignment | ${motivation_status} | ${motivation_finding} | ${motivation_action} |

## Comparison To Closest Prior Methods

Use only prior-method context already gathered in the Motivation stage.

| Prior Method | Shared Idea | Difference | Why The Difference Matters |
| --- | --- | --- | --- |
| ${prior_method} | ${shared_idea} | ${difference} | ${importance} |

## User Confirmation Needed

| Item | Reason | Decision Needed |
| --- | --- | --- |
| ${confirmation_item} | ${confirmation_reason} | ${decision_needed} |
